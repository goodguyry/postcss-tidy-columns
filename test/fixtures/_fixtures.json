{
  "tests": [
    {
      "description": "tidy-span",
      "skip": false,
      "options": {},
      "fixtures": {
        "input": "fixtures/span.css",
        "expected": "fixtures/span.expected.css"
      }
    },
    {
      "description": "tidy-span w/ gap margin",
      "skip": false,
      "options": {},
      "fixtures": {
        "input": "fixtures/span-gap.css",
        "expected": "fixtures/span-gap.expected.css"
      }
    },
    {
      "description": "tidy-offset-left",
      "skip": false,
      "options": {},
      "fixtures": {
        "input": "fixtures/offset-left.css",
        "expected": "fixtures/offset-left.expected.css"
      }
    },
    {
      "description": "tidy-offset-left (negative)",
      "skip": false,
      "options": {},
      "fixtures": {
        "input": "fixtures/offset-left-negative.css",
        "expected": "fixtures/offset-left-negative.expected.css"
      }
    },
    {
      "description": "tidy-span + tidy-offset-left",
      "skip": false,
      "options": {},
      "fixtures": {
        "input": "fixtures/span-offset.css",
        "expected": "fixtures/span-offset.expected.css"
      }
    },
    {
      "description": "full-suite w/ media query",
      "skip": false,
      "options": {},
      "fixtures": {
        "input": "fixtures/full-suite.css",
        "expected": "fixtures/full-suite.expected.css"
      }
    },
    {
      "description": "tidy-columns (offset + span shorthand)",
      "skip": false,
      "options": {},
      "fixtures": {
        "input": "fixtures/columns.css",
        "expected": "fixtures/columns.expected.css"
      }
    },
    {
      "description": "tidy-columns w/ fractional offset",
      "skip": false,
      "options": {},
      "fixtures": {
        "input": "fixtures/columns-fraction.css",
        "expected": "fixtures/columns-fraction.expected.css"
      }
    },
    {
      "description": "tidy-columns w/ gaps",
      "skip": false,
      "options": {},
      "fixtures": {
        "input": "fixtures/columns-gap.css",
        "expected": "fixtures/columns-gap.expected.css"
      }
    },
    {
      "description": "tidy-columns w/ negative offset",
      "skip": false,
      "options": {},
      "fixtures": {
        "input": "fixtures/columns-negative-offset.css",
        "expected": "fixtures/columns-negative-offset.expected.css"
      }
    },
    {
      "description": "tidy-columns w/ zero offset",
      "skip": false,
      "options": {},
      "fixtures": {
        "input": "fixtures/columns-zero-offset.css",
        "expected": "fixtures/columns-zero-offset.expected.css"
      }
    },
    {
      "description": "tidy-columns w/ inherit offset",
      "skip": false,
      "options": {},
      "fixtures": {
        "input": "fixtures/columns-inherit-offset.css",
        "expected": "fixtures/columns-inherit-offset.expected.css"
      }
    },
    {
      "description": "tidy-offset-right",
      "skip": false,
      "options": {},
      "fixtures": {
        "input": "fixtures/offset-right.css",
        "expected": "fixtures/offset-right.expected.css"
      }
    },
    {
      "description": "tidy-offset-right (negative)",
      "skip": false,
      "options": {},
      "fixtures": {
        "input": "fixtures/offset-right-negative.css",
        "expected": "fixtures/offset-right-negative.expected.css"
      }
    },
    {
      "description": "tidy-offset shorthand",
      "skip": false,
      "options": {},
      "fixtures": {
        "input": "fixtures/offset.css",
        "expected": "fixtures/offset.expected.css"
      }
    },
    {
      "description": "tidy-offset shorthand (inherit first value)",
      "skip": false,
      "options": {},
      "fixtures": {
        "input": "fixtures/offset-inherit.css",
        "expected": "fixtures/offset-inherit.expected.css"
      }
    },
    {
      "description": "tidy-offset shorthand (zero first value)",
      "skip": false,
      "options": {},
      "fixtures": {
        "input": "fixtures/offset-zero.css",
        "expected": "fixtures/offset-zero.expected.css"
      }
    },
    {
      "description": "tidy-offset shorthand (no second value)",
      "skip": false,
      "options": {},
      "fixtures": {
        "input": "fixtures/offset-single-value.css",
        "expected": "fixtures/offset-single-value.expected.css"
      }
    },
    {
      "description": "tidy local atrules",
      "skip": false,
      "options": {},
      "fixtures": {
        "input": "fixtures/local-atrules.css",
        "expected": "fixtures/local-atrules.expected.css"
      }
    },
    {
      "description": "tidy-span() function",
      "skip": false,
      "options": {},
      "fixtures": {
        "input": "fixtures/function-span.css",
        "expected": "fixtures/function-span.expected.css"
      }
    },
    {
      "description": "tidy-span() function (nested in calc())",
      "skip": false,
      "options": {},
      "fixtures": {
        "input": "fixtures/function-span-calc.css",
        "expected": "fixtures/function-span-calc.expected.css"
      }
    },
    {
      "description": "tidy-offset() function",
      "skip": false,
      "options": {},
      "fixtures": {
        "input": "fixtures/function-offset.css",
        "expected": "fixtures/function-offset.expected.css"
      }
    },
    {
      "description": "tidy-offset() function (nested in calc())",
      "skip": false,
      "options": {},
      "fixtures": {
        "input": "fixtures/function-offset-calc.css",
        "expected": "fixtures/function-offset-calc.expected.css"
      }
    },
    {
      "description": "zero-gaps: should correctly equate columns with no gap.",
      "skip": false,
      "options": {
        "columns": 12,
        "siteMax": "75rem",
        "edge": "10px"
      },
      "fixtures": {
        "input": "fixtures/zero-gaps.css",
        "expected": "fixtures/zero-gaps.expected.css"
      }
    },
    {
      "description": "test-one: should replace `tidy-` properties.",
      "skip": false,
      "options": {},
      "fixtures": {
        "input": "fixtures/web/test-one.css",
        "expected": "fixtures/web/test-one.expected.css"
      }
    },
    {
      "description": "test-two: should replace `tidy-` properties.",
      "skip": false,
      "options": {},
      "fixtures": {
        "input": "fixtures/web/test-two.css",
        "expected": "fixtures/web/test-two.expected.css"
      }
    },
    {
      "description": "test-three: should correctly equate rows with multiple columns.",
      "skip": false,
      "options": {},
      "fixtures": {
        "input": "fixtures/web/test-three.css",
        "expected": "fixtures/web/test-three.expected.css"
      }
    }
  ]
}
